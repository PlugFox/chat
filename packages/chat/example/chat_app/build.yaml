targets:
  $default:
    sources:
      - $package$
      - lib/**
      - pubspec.yaml
      - test/**
    builders:
      pubspec_generator:
        options:
          output: lib/src/common/constant/pubspec.yaml.g.dart
